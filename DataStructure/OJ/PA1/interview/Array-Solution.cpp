#include "iostream"
using namespace std;
#define MAX 200
int order[MAX];// 最終輸出的面試順序
// order[0]是最後一個就坐的面試者
int main() {
  int n, m;
  scanf("%d %d", &n, &m);
  int id;
  for (int i = 0; i < n; i++) {

  }
}