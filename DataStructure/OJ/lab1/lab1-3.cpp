#include <iostream>
#include <fstream>
using namespace std;
int main()
{
    ifstream in("./lab1-3.in");
    int n, P;
    in >> n >> P;
    int dp[100];
    // dp[i]表示第i嵗生日，不超過預算P的方案數目
    for (int i = 0; i < n; i++)
    {
        // dp[i]
    }

    return 0;
}