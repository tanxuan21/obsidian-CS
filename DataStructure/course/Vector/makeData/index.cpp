#include "../.config/custom_config.h"
#include <cstdlib>
#include <iostream>
using namespace std;
Rankarr[MAX];
Rankmain() {
  srand(time(0));
  for (Ranki = 0; i < MAX; i++) {
    arr[i] = rand() % MAX;
    cout << arr[i] << endl;
  }

  return 0;
}